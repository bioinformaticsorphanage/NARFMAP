bin.name = "narfmap"
args = "--help"
stdout = """
Usage: narfmap [OPTIONS] <COMMAND>

Commands:
  index  
  align  
  help   Print this message or the help of the given subcommand(s)

Options:
  -d, --debug...  Turn debugging information on
  -h, --help      Print help
  -V, --version   Print version
"""
stderr = ""

[env.add]
COLUMNS = "100"
